{
  "name": "colive-api",
  "private": true,
  "version": "1.0.0",
  "description": "colive.fun backend: hosted username+password accounts + state sync + shared houses",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}